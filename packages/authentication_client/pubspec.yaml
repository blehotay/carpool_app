name: authentication_client
description: Authentication client for Firebase Auth
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  equatable: ^2.0.5
  firebase_auth: ^5.0.0
  firebase_core: ^3.0.0

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.0
  very_good_analysis: ^6.0.0
